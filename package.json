{
  "private": true,
  "description": "FlowDrop monorepo",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,svelte,json,css,md,yaml,yml}": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "15.5.2",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
